<section class="wishlist-page container" data-wishlist-page>
  <header class="wishlist-page__header">
    <h1>{{ page.title | default: 'Wishlist' }}</h1>
  </header>

  <div data-wishlist-content>
    <p>Loading wishlist...</p>
  </div>
</section>